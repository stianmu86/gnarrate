# Narrate — Modal.com Python Worker Secrets
# Set via: modal secret create narrate-secrets <KEY>=<VALUE> ...
# These are NOT stored in a file in production — this is a reference only.
#
# ⚠️  Uses SUPABASE_SERVICE_ROLE_KEY for full write access.
#     NEVER use the anon key here.

SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
